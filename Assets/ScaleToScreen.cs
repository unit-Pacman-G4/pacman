using UnityEngine;
using System.Collections;

public class ScaleToScreen : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}
	
	// Update is called once per frame
	void Update () {
		/*Debug.Log (Screen.width);
		int screen = Screen.width;
		int origWidth = 1380 + 830;
		float diff = ((float)(Screen.width + Screen.height) / origWidth);
		Debug.Log (diff);
		transform.localScale = new Vector3 (diff, diff, 1);*/
	
	}
}
